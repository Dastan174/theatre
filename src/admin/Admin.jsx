import React from "react";
import { useMain } from "../context/MainContext";

const Admin = () => {
  // const { postActors } = useMain();
  return (
    <div>
      {/* <button onClick={() => postActors()}>Get Actors</button> */}
    </div>
  );
};

export default Admin;
